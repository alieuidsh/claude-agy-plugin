{
  "name": "claude-agy-plugin-runtime",
  "version": "2.1.0",
  "private": true,
  "description": "Runtime dependency manifest for the agy Claude Code plugin. node-pty lets the companion drive the agy CLI through a synthesized console (ConPTY). Installed automatically on first run.",
  "dependencies": {
    "node-pty": "^1.0.0"
  }
}
